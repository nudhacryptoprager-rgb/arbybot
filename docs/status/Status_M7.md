# Status: M7 (Triangular Feasibility)

**Status**: **E1.59 soak verification + bridge-refresh fix LANDED (2026-05-05T15:40Z → 16:10Z).** Two soaks run this session. Root causes found and fixed: (1) `m7/orderflow/simulation.py` default changed from `BACKEND_TENDERLY` → `BACKEND_RPC_FORK` — was causing Tenderly 429s and WS drops on every soak where `ARBY_SIM_BACKEND` was unset; (2) `m7/orderflow/loop_runner.py` bridge-refresh added — `_bridge` was read only once at hot-lane init time (before cold lane wrote its first window ~15 min later), so `cold_immediate_sim_input_count` stayed 0 even when cold found profitable candidates; fix: re-read `_read_cold_hot_bridge()` each WS iteration before `queue_cold_executable_for_sim`. **Soak 2 results** (bridge-refresh fix applied, 15:40Z → 16:10Z, base, 5/5 alive 0 crashes): DISCOVERY hot `loop_iter=4`, `ws=connected`, `events=73`, `bridge_loaded=8`, `cold_imm_input=3`, **`cold_imm_profitable=3`**, `preflight_aggregator.candidates_total=3, blocked=0`, all 6 E1.59 keys PRESENT in rollup (`sim_backend=rpc_fork`), zero 429 windows in provider_throttle. **Tests**: 6 tests updated to reflect `rpc_fork` as new default (Tenderly now opt-in via `ARBY_SIM_BACKEND=tenderly`); full suite **4730 PASS / 6 skipped / 0 failures**. `simulation.py` docstring updated: "Default: `rpc_fork` (Tenderly is opt-in)". **PROD hot WS**: both soaks showed `failed_other` on first hot window (WS timeout 10 min), then `connected` on subsequent iterations — normal for dRPC WS reconnect. **Open item**: PROD hot lane gets `events=0` on first few iterations after WS reconnect; DISCOVERY hot lane gets `events=73` and is the primary evidence lane for this session. **Operator next**: optionally run longer (1h+) soak with PROD WS reconnect to see PROD hot `cold_imm_profitable > 0` — expected same behavior as DISCOVERY once bridge is fresh. docs_reread_confirmed: true.

## E1.59 runtime wiring — prior status entry below.

**Status**: **E1.59 runtime wiring LANDED (offline session, post-E1.59 module batch).** All 6 E1.59 instrumentation modules connected to the live runtime loop. Changes: (1) `m7/orderflow/hot_runtime_artifacts._update_hot_rollup` — merges snapshots from `revert_taxonomy`, `disc_to_prod_pool_promotion`, `preflight_aggregator`, `canary_rehearsal`, `sim_v1_dry_compare`, and `provider_throttle` into the hot rollup artifact every window (gated by ARBY_* env per module, default OFF); (2) `m7/orderflow/cold_immediate_sim.queue_cold_executable_for_sim` — added `revert_taxonomy.record` for sim errors, `preflight_aggregator.record` for guard-passed candidates, `canary_rehearsal.rehearse` after gate, and `sim_v1_dry_compare.compare_results` sidecar feeding gate's `sim_output_samples`; (3) `m7/orderflow/loop_runner.py` hot lane — added `pool_state_http_feed.poll_and_feed` for bounded PTT pool set (HTTP RPC), and `disc_to_prod_pool_promotion.observe_profitable` reading DISC cold bridge. **Tests**: +9 new wiring verification tests in `tests/unit/test_e1_59_runtime_wiring.py` (4720 → **4729 PASS / 6 skipped**). `check_repo_safety.py --allow-intent-edit` PASS 0 warnings. **All wiring is non-blocking** (wrapped in `try/except Exception: pass`); never alters gate outcome. docs_reread_confirmed: true.

## E1.59 offline module batch — prior status entry below.

**Status**: **E1.59 reviewer 10-step batch LANDED (offline-only session, post-E1.58).** All 10 reviewer fix steps from the post-3h-soak verdict implemented sequentially with paired unit tests, default OFF, additive-only public API. **New modules** (all gated by ARBY_* env, default OFF; live submit paths intentionally NOT implemented per AGENTS.md): #1 `m7/orderflow/hot_runtime_artifacts._stamp_soak_baseline_at_path` + `mark_supervisor_start` (clears stale `supervisor_end_utc`, stamps fresh `soak_baseline_at`/`soak_baseline_session_id`/counters_snapshot for both PROD and DISC rollups; wired into `scripts/start_nonstop_runtime.py` after `for p in processes: p.start()`); #2 `core/provider_throttle.py` (per-method `TokenBucketRateLimiter` budgets logs:20rps/calls:30rps/sim:5rps + 408/429 circuit breaker with cooldown ladder 1→60s, gated by `ARBY_PROVIDER_THROTTLE=1`); #3 `chains/flashblocks_http.py` (HTTP-only Flashblocks lane via `eth_getLogs` `pending` block tag, refuses empty-address scans, integrates throttle, gated by `ARBY_FLASHBLOCKS_HTTP_LANE=1`); #4 `execution/sim_v1_dry_compare.py` (dataclass-based dry compare of rpc_fork vs eth_simulateV1 with reason normalization on first 2 colon-segments, recent-disagree buffer cap 30, gated by `ARBY_SIM_V1_DRY_COMPARE=1`); #5 `m7/orderflow/pool_state_http_feed.py` (bridges flashblocks_http into `pool_price_state.feed_raw_logs` with default V3-Swap+V2-Sync topic OR-list, gated by `ARBY_POOL_STATE_HTTP_FEED=1`); #6 `m7/orderflow/revert_taxonomy.py` (priority-ordered classifier into 7 buckets STF/HTTP_408/HTTP_429/OUT_OF_GAS/NO_DATA/EXECUTION_REVERTED/OTHER, max 20 samples/bucket, gated by `ARBY_REVERT_TAXONOMY=1`); #7 `m7/orderflow/disc_to_prod_pool_promotion.py` (DISC→PROD pool-level promotion with full routing identity, case-insensitive dedup, TTL via `ARBY_POOL_PROMOTION_TTL_S` default 600s, sorted active list, gated by `ARBY_POOL_PROMOTION=1`); #8 `m7/orderflow/preflight_aggregator.py` (rollup-friendly aggregator over E1.58 `run_preflight` outcomes, reason-prefix normalization strips `:N<M`, recent-blockers cap 30, gated by `ARBY_EXECUTION_PREFLIGHT=1`); #9 `m7/orderflow/canary_rehearsal.py` (combines `submit_canary` dry_run + `apply_runtime_pnl_guard` so paper-signing soak exercises kill-switch + 1-wei canary path, recent-rehearsals cap 20, gated by `ARBY_CANARY_REHEARSAL=1`). **Tests**: +90 new (4630→**4720 PASS / 6 skipped**, 0 failures). `check_repo_safety.py --allow-intent-edit` PASS 0 warnings. **Live submit paths unchanged**: `private_submitter`/`canary_submitter` live branches still return `LIVE_*_NOT_IMPLEMENTED`. **Operator next**: 2-3h verification soak with new flags ON (ARBY_PROVIDER_THROTTLE=1 ARBY_FLASHBLOCKS_HTTP_LANE=1 ARBY_POOL_STATE_HTTP_FEED=1 ARBY_REVERT_TAXONOMY=1 ARBY_POOL_PROMOTION=1 ARBY_EXECUTION_PREFLIGHT=1 ARBY_CANARY_REHEARSAL=1) on top of E1.58 paper-signing baseline; expected effects: reduced 408/429 windows from breaker, non-zero `pool_price_state.updates_total` from HTTP feed, populated revert taxonomy buckets, populated DISC→PROD promotion list. docs_reread_confirmed: true.

## E1.58 3h runtime verification — prior status entry below.

**Status**: **E1.58 3h runtime verification REACHED (2026-05-05T10:05:24Z -> 13:05:29Z, base, session_id 94894a30 reused).** 5/5 children stayed alive until clean shutdown, `crash_restarts=0`, stderr empty, post-soak `check_repo_safety.py --allow-intent-edit` PASS 0 warnings. This soak ran with `ARBY_COLD_IMMEDIATE_SIM=1`, `ARBY_COLD_IMMEDIATE_MIN_NET_BPS=0`, `ARBY_PAPER_SIGNING=1`, PROD/DISC `rpc_fork`, Tenderly disabled. **PROD HOT**: `events_seen_total=1107`, `fast_path_scored_total=961`, `fast_path_positive_total=71`, `cold_immediate_sim_input_total=332`, `attempted=322`, `passed=69`, `profitable=69`, `cold_immediate_roundtrip_attempted_total=69`, **`cold_immediate_roundtrip_profitable_total=7`**, **`cold_immediate_submit_ready_total=7`**, **`roundtrip_profitable_total=7`**, **`submit_ready_total=7`**, `roundtrip_profit_bps_best/median/worst=982.8267/980.911/-30.1319`, `rate_metrics.profitable_event_rate_per_hour=2.1082`, `scoring_blackhole_rate=0.0`, `l1_fee_source_last=onchain`, `external_provider_blocker_total=0`, `session_ws_failed_429_windows=41`. **DISCOVERY**: `cold_immediate_roundtrip_profitable_total=35`, `submit_ready_total=35`, `roundtrip_profitable_total=35`, `roundtrip_profit_bps_best/median/worst=1397.5115/326.4385/-135.9316`, `session_ws_failed_429_windows=37`. E1.58 submit-ready wiring and roundtrip-bps buffer are now proven live, not only unit-tested. **Important boundary**: this is simulated/paper canonical readiness, not real on-chain profit; live submit paths remain intentionally unimplemented/unauthorized. Residual blockers: dRPC 408/429 pressure, no hot `pool_price_state` updates in this run, high cold-immediate revert volume (`REVERT:STF` / `REVERT:unknown:no_data`), Flashblocks preflight warning 405, rolling session hygiene issue where old `session_id/supervisor_end_utc` persisted until final shutdown. docs_reread_confirmed: true.

**Status**: **E1.58 multi-step session LANDED (2026-05-05).** Steps #1 (runtime wiring), #2 (preflight), #3 (canary dry-run), #4 (fee-tail int-cast — prior commit), #5 (private submitter dry-run), #6 (max-loss kill-switch), #7 (PnL accounting dry-run), #8 (CI roundtrip bps to buffer), #9 (docs) ALL implemented in one session per user directive. **Code changes** (additive, public API stable): `m7/orderflow/loop_runner.py` extracts CI gate `submit_ready` + `roundtrip_profit_bps_values` into counters; `m7/orderflow/hot_runtime_artifacts.py` accumulates `cold_immediate_submit_ready_total`, bumps top-level `submit_ready_total` by CI submit_ready, drains CI bps into `_roundtrip_profit_bps_all` (so `roundtrip_profit_bps_best/worst/median` are no longer null). **New modules** (default OFF, dry-run-only): `m7/orderflow/preflight.py` (router/balance/allowance ERC-20 checks behind `ARBY_EXECUTION_PREFLIGHT=1`); `m7/orderflow/runtime_pnl_guard.py` (cumulative PnL + max-loss kill-switch via `ARBY_MAX_LOSS_WEI`); `execution/private_submitter.py` (Flashbots/MEV-Share/Blink dry-run, refuses live submit until wired); `execution/pnl_accounting.py` (balance snapshots + USD PnL); `execution/canary_submitter.py` (1-wei self-transfer dry-run). **Tests**: +49 new (4581→4630 PASS, 6 skipped, 0 failures). **Operational verification**: 15-min fresh soak (2026-05-05T09:12:38Z → 09:27:40Z, base, ARBY_PAPER_SIGNING=1) — supervisor 5/5 alive throughout, 0 crashes, 0 restarts; rollup writers active; however, hot WS lane captured 0 canonical events in this short window (`cycles_completed=0`, all funnel counters 0 on both lanes) — expected: prior E1.57 evidence required full hour to first non-zero `cold_immediate_roundtrip_profitable_total`. Wiring correctness for new counters is proven at unit-test level by `test_e1_58_fix1_ci_submit_ready_into_total` and `test_e1_58_fix8_ci_roundtrip_bps_into_buffer` (both PASS) which directly assert propagation into `submit_ready_total` / `roundtrip_profit_bps_best/worst/median`. Full runtime evidence requires ≥1h soak (next operator action). Steps #3/#5/#7 are scaffolds — live submission paths intentionally return `*_NOT_IMPLEMENTED` to enforce AGENTS.md "no real submit without authorization". `check_repo_safety.py` PASS 0 warnings post-docs-trim. docs_reread_confirmed: true.

## E1.58 offline fix step #4 LANDED + E1.57 BREAKTHROUGH — prior status entry below.

**Status**: **E1.58 offline fix step #4 LANDED + E1.57 BREAKTHROUGH confirmed (2026-05-04T20:10:09Z).** Simulated canonical roundtrip profit repeatedly proven (1h + 2h soaks); real execution pending. `submit_ready_total=0` because `ARBY_PAPER_SIGNING=1` not set in soak command — paper-signing mode not yet enabled. **E1.58 fix step #4**: fee-tail type mismatch fixed — `best_buy_fee` from Discovery lane arrived as string `"1570"` (not int), causing `_AERODROME_CL_KNOWN` set-membership to return False → `UNSUPPORTED_FEE_TIER:1570` instead of `SLIPSTREAM_PENDING_LOOKUP:1570`. Fix: int-cast in pre-sim fee check; 2600 added to PRE_SIM_SKIP `_AERODROME_CL_KNOWN`. 4 new tests. **4581 PASS / 6 skipped**. `check_repo_safety.py` PASS 0 warnings. Next soak must include `ARBY_PAPER_SIGNING=1` to exercise `submit_ready_total > 0` path. docs_reread_confirmed: true.

## E1.57 1h soak BREAKTHROUGH — prior status entry below.

**Status**: **E1.57 1h soak BREAKTHROUGH (2026-05-04T19:10:07Z → 20:10:09Z, base, session 2f5d116d).** 5/5 alive, 0 crashes, 0 restarts across 60 min. **PROD HOT**: `cold_immediate_sim_input_total=312`, `attempted=260`, `guard_passed=242`, `pre_sim_skip=15`, `revert=143`, `sim_profitable=84`, `cold_immediate_roundtrip_attempted_total=14`, **`cold_immediate_roundtrip_profitable_total=4`** ✓, **`roundtrip_profitable_total=4`** ✓, `roundtrip_success_total=4` ✓, `cold_immediate_profitable_waiting_canonicalization` CLEARED ✓. **DISCOVERY**: `cold_immediate_roundtrip_profitable_total=8`, `roundtrip_profitable_total=8`, `waiting_canonicalization` CLEARED. **L1 fee diagnostic LIVE on both lanes**: `l1_fee_source_last="onchain"`, `l1_fee_wei_last≈3.47e9 wei` (~3.47 Gwei), `l1_fee_calldata_len=228`, `l1_fee_calldata_kind="swaprouter02_representative"`. Reviewer breakthrough criterion #3 MET. Minor regression (Discovery fee tail — **FIXED in E1.58**): was `UNSUPPORTED_FEE_TIER:1570/9500/7500` → fixed by int-cast in pre-sim check. Code identity: 4577 PASS / 6 skipped (at soak time) → 4581 PASS after E1.58 step #4. docs_reread_confirmed: true.

## E1.57 fix steps #4+#8 — prior status entry below.

**Status**: **E1.57 fix steps #4+#8 LANDED (offline-only session, 2026-05-04).** L1 fee diagnostic fields CLOSED: `ExecutionGateResult` now carries `l1_fee_wei`, `l1_fee_source` ("onchain"|"fallback_zero"|"not_applicable"), `l1_fee_calldata_len`, `l1_fee_calldata_kind`; `run_execution_gate` populates them; `_update_hot_rollup` writes them to rolling artifact as `l1_fee_wei_last`, `l1_fee_source_last`, `l1_fee_calldata_len`, `l1_fee_calldata_kind` — reviewer can now confirm L1 fee is live from artifact. `roundtrip_success_total` consistency CLOSED (fix step #8): when CI roundtrip profitable merge fires, `roundtrip_success_total` is also incremented by same amount (profitable ⊆ success). +5 new tests. Full suite: **4577 PASS / 6 skipped** (+5 vs 4572). `check_repo_safety.py` PASS 20/20, 0 warnings. Next: 1h verification soak (ENV: ARBY_COLD_IMMEDIATE_SIM=1, ARBY_SIM_BACKEND_PROD=rpc_fork) to confirm `l1_fee_source_last="onchain"` in rolling artifact and `cold_immediate_roundtrip_profitable_total > 0` clears `waiting_canonicalization`. docs_reread_confirmed: true.

## E1.56 fix steps #3+#4+#6 — prior status entry below.

**Status**: **E1.56 fix steps #3+#4+#6 LANDED (offline-only session).** Discovery fee tail CLOSED: fee 7500 added to all 3 `_AERODROME_CL_KNOWN` sets; L1480 pre-sim classification now **unconditional** (no `get_dex_config` dependency → eliminates intermittent `UNSUPPORTED_FEE_TIER:7500/9500/1570` in Discovery subprocess). New rollup fields: `cold_immediate_profitable_waiting_canonicalization=True` (fix step #3 — separate short boolean alongside `cold_immediate_profitable_not_canonical`) and `cold_immediate_sim_revert_samples_recent` list (fix step #6 — last 30 pair/fee/reason samples across windows). Temp patch scripts deleted. Full suite: **4559 PASS / 6 skipped** (+5 vs 4554). `check_repo_safety.py` PASS 20/20. Next: 1h verification soak to confirm no `UNSUPPORTED_FEE_TIER` in Discovery histogram + revert samples populated in rollup. docs_reread_confirmed: true.

## E1.56 2h soak BREAKTHROUGH — prior status entry below.

**Status**: **E1.56 Step 1 — 2h soak BREAKTHROUGH (2026-05-04T13:23:42Z → 15:23:45Z, --m7-cold-pause 3).** 5/5 alive, 0 crashes. **MAIN HOT** (65 windows): `cold_immediate_sim_input_total=242`, `attempted=190`, `guard_passed=172`, `pre_sim_skip=15`, `revert=87`, `sim_passed_total=70` ✓, `sim_profitable_total=70` ✓, `cold_immediate_profitable_not_canonical=True` ✓ (reviewer verdict ACTIVE). **DISCOVERY** (63 windows): `input=290`, `attempted=240`, `passed=29`, `profitable=29`, verdict ACTIVE. `roundtrip_profitable_total=0` (canonical hot WS path) — gas blocker on hot WS (919/919 matched_then_gas_rejected). 9500 fee classified correctly (no UNKNOWN errors). 1570 fee shows as `SLIPSTREAM_PENDING_LOOKUP:1570` (4 occurrences — proper Slipstream path, not UNSUPPORTED). Cold lane found profitable pairs continuously: TIG/WETH (+1489 bps), PING/WETH (+462 bps), BSHIB/WETH (+682 bps), SHRMN/WETH (+342 bps), TN100x/WETH (+40 bps). Step 1 + fix steps #4 + #8 fully validated in production rollups. docs_reread_confirmed: true.

## Next steps (historical — pre-E1.45)

1. M7 Arbitrum mainline FROZEN. Phases 1-4.5 DONE (E1.17-E1.26). Phase 5 (sim revert diagnosis) and Phase 6 (Flashblocks) superseded by E1.45+ cadence improvements. Triangular arb CLOSED (-14.16 bps).

## E1.56 prior — fix steps #4 + #8 LANDED.

## E1.56 Step 1 re-soak — **P0b CONFIRMED; BUG-1 runtime verified (attempted_total=33); BUG-2 fixed; diagnostic counters added.** 30-min re-soak (2026-05-04T12:37:39Z → 13:07:39Z, ARBY_COLD_IMMEDIATE_SIM=1): 5/5 alive, `cold_immediate_sim_input_total=50`, `cold_immediate_sim_attempted_total=33` — **P0b criterion MET** (profit_guard now passes candidates post BUG-1 fix). `passed_total=0` — expected: stale cold scan price → rpc_fork REVERT:STF. **BUG-2 FIXED**: fee_tier=1570 added to `SLIPSTREAM_FEE_TO_TICKSPACING` (→ tickSpacing=100) and all 3 `_AERODROME_CL_KNOWN` sets in `execution_gate.py`. **Diagnostic counters (issue #3) added**: `cold_immediate_guard_passed`, `cold_immediate_profit_guard_rejected`, `cold_immediate_pre_sim_skip`, `cold_immediate_sim_revert` — now in rollup JSON from next soak onward. Full suite: **4552 PASS / 6 skipped** (pre-fix steps #4/#8). `check_repo_safety` 20/20 PASS. **goal_status: REACHED (P0b confirmed); fix steps #4+#8 applied above**. docs_reread_confirmed: true.

## E1.56 prior — **STRATEGY_GATING DIAGNOSED + POOL-LEVEL VISIBILITY/QUARANTINE LANDED.** Reviewer pushed back on prior "market_blocker only" framing: cold lane finds profitable pairs (FUN/USDC +997 bps, B3/WETH +425 bps) but hot lane requires a matching hot WS event before any sim attempt — producing `roundtrip_profitable=0` and `sim_attempted=0` even when cold truth is positive. **Refined classification: profitability is BLOCKED by `MARKET_WINDOW + STRATEGY_GATING`** (not market alone). MARKET_WINDOW: PENGACHU/WETH dominates Base WS; gas exceeds gross profit by ~2 bps. STRATEGY_GATING: hot execution is event-driven on the hot pool stream — it never re-sims cold-positive entries against current state without a triggering WS event, and pool-level recurrent gas-loser pools were only quarantined at family granularity (one bad pool poisons family or vice versa). **Landed (E1.56 prior)**: Step 6 — `cold_positive_pools_count`, `cold_positive_pool_seen_in_hot_count`, `pool_address_mismatch_count` in `hot_gap_debug` for STRATEGY_GATING visibility; Step 7 — pool-level gas-hopeless quarantine (per-pool consecutive `GAS_EXCEEDS_GROSS` streak; quarantine after `ARBY_POOL_GAS_HOPELESS_STREAK` windows, default 3, set 0 to disable; persisted across windows in cold-hot bridge). Tests +11. **Now superseded by Step 1 above.**

## E1.55 — Prior — **HOT_REGISTRY_EMPTY / BRIDGE_NOT_INGESTED ROOT CAUSE FIXED AND VALIDATED + 30-MIN PROD SOAK PASS.** 30-min PROD soak (2026-05-04T06:34:43Z → 07:04:45Z, PROD=rpc_fork): supervisor 5/5 alive, crash_restarts=0, clean_restarts=0. hot_windows=13, events_total=772, fast_scored_total=184, positive=0 (best_net_bps=-2.15, market condition). bridge_ptt_raw grew 35→65 at window 9 (cold bridge updated at 06:50:35Z, FUN/USDC=+997bps, B3/WETH=+425bps found by cold lane). E1.55 acceptance criteria ALL MET throughout all 13 windows: `bridge_ptt_raw_count>0 ✓, bridge_pool_address_hit_count>0 ✓, admitted_to_scoring>0 ✓, fast_score_scored>0 ✓`. Gas blocker: 184/184 scored events → gas_rejected (matched_bridge_then_gas_rejected). Bottleneck: PENGACHU/WETH pool dominates WS event stream; gas cost exceeds gross profit by ~2 bps (market condition, not a code bug). Cold bridge confirms profitable pairs (FUN/USDC +997 bps, B3/WETH +425 bps) but hot WS event stream does not deliver from those pools in this time window. **goal_status: REACHED (scoring ingress) / BLOCKED (profitability)** — refined to MARKET_WINDOW + STRATEGY_GATING in E1.56 above. Tests: 6 tests in `test_e1_55_bridge_cache_reload.py` — all PASS. Full suite (at E1.55 close): **4526 PASS / 6 skipped**. docs_reread_confirmed: true.

E1.54 soak (08:23-08:53Z, rpc_fork) revealed 100% REJECT_NOT_IN_HOT_REGISTRY: events_seen=421, fast_path_scored=0. Root cause: cold-start race condition — when rolling directory is cleared, hot process starts with empty `_pool_token_cache.json`, calls `load_persistent_pool_token_cache()` at import time (file absent → `_PERSISTENT_CACHE_LOADED=True`, 0 entries loaded). Cold lane writes `_pool_token_cache.json` only after ~900s (first window). Hot windows at T+0 and T+601s both read empty bridge/cache → `score_backrun_fast` returns None for all events (L1382: `if _cached_pool is None: return None`). Fix: `force_reload_persistent_pool_token_cache()` added to `m7/orderflow/resolve.py`; hot startup in `loop_runner.py` now calls force-reload when `_bridge_cache_count==0` and `_pool_token_cache` is empty. Also added: `bridge_file_exists / bridge_ptt_raw_count / bridge_mtime_age_s / persistent_cache_forced_reload_count` diag fields to hot_gap_debug; SCORING_BLACKHOLE guard (logger.WARNING when events>0 and fast_path_scored==0). Control soak evidence: PROD events=594 fast_scored=105; DISC events=606 fast_scored=105. NOTE: `bridge_cache_populated` may remain 0 (legacy counter not a primary criterion). New bottleneck: `matched_bridge_then_gas_rejected=105`, `best_net_bps=-2.15` — gas cost exceeds gross profit by ~2 bps (market condition). docs_reread_confirmed: true.

## E1.54 — Prior — **`ws_exception "name 'os' is not defined"` ROOT CAUSE FIXED.** During E1.53 PROD→rpc_fork relaunch soak, every WS hot/cold window terminated after 2-24 blocks. Fix: `import os` added to module-level imports of `m7/orderflow/scoring_parallel.py`. 30m soak (08:23:09Z) confirmed: 5/5 alive, crash_restarts=0. BUT revealed HOT_REGISTRY_EMPTY — see E1.55 above. **goal_status: IN_PROGRESS** → superseded by E1.55. docs_reread_confirmed: true.

## E1.52 — Prior — **E1.52 — 22h soak analyzed, Tenderly key updated.** Publicnode WS stable (ws_fallback=False 1132 iter), ZLR fix confirmed (bridge_focused 0→389), 21 sim → HTTP 403 old key. New key HTTP 200 /user+/project but NOT proven for /simulate. **goal_status: IN_PROGRESS**. docs_reread_confirmed: true.

## E1.51 — Prior — **E1.51 AttributeDict + HexBytes sink fixes LANDED + canary #8 LIVE PROOF — `feed_raw_logs` now correctly decodes `web3.eth.get_logs()` `AttributeDict` logs (Root Cause 3: `isinstance(lg, dict)` was False for `AttributeDict` → silent skip of every log). Canary #8 cold lane confirmed `pps_v3_updates=12, pps_skipped=0` — first non-zero live pps result. Three root causes resolved: RC1=HexBytes TypeError fix (prior session), RC2=drpc 429 infra constraint (structural), RC3=isinstance Mapping fix (this session). Hot rollup `updates_total` stays 0 because hot lane exits before HTTP fallback (drpc 429 storm); cold lane proves the sink works.** Verification: `py -3.11 -m pytest tests/unit -q` **4510 passed / 6 skipped / 1 warning** in 115s (+3 new tests: 2 HexBytes + 1 AttributeDict). `check_repo_safety.py --allow-intent-edit` PASS 0 warnings 20 gates. **goal_status: IN_PROGRESS** — `REACHED` requires working WS so hot lane gets raw_logs. docs_reread_confirmed: true.

## E1.51 slices 3b/3c surfacing + HexBytes fix — Latest (LANDED.) `*, chain: Optional[str] = None` kwarg to `m7/orderflow/resolve.py::extract_pool_state_for_sim` (backward compatible, default behaviour unchanged) — when `chain` is provided AND `ARBY_REGISTRY_FIRST_POOL_STATE=1` (default off for safety), the function consults `PoolPriceStateRegistry` first and falls back to `MulticallBatcher.batch_full_pool_data` only for cold pools (registry hits stamp `"source": "local_registry"`). Test shield: `tests/unit/test_extract_pool_state_registry_first.py` 7 cases, all passing. slice-3c surfaces registry counters in the rolling artifact for canary verification — the block is now written by **all three** rollup writers in `m7/orderflow/hot_runtime_artifacts.py`: (1) `_update_hot_rollup` (windowed counter increment path), (2) `heartbeat_hot_rollup_cycle` (mid-cycle periodic flush, ~30s cadence), (3) `flush_rollup_shutdown`/`_flush_rollup_at_path` (clean child exit path). The first canary attempt (#3) showed `pool_price_state: None` because only `_update_hot_rollup` carried the block and the WS recv loop never delivered events to that path; canary #4 with the writer fan-out confirmed the block lands at every flush boundary. All write paths read counters via the same `m7.orderflow.pool_price_state.get_registry().counters()` snapshot and never raise (defensive try/except). Verification: `pytest tests/unit -q` 4502 PASS; live canary #4 rollup carries `pool_price_state` block. docs_reread_confirmed: true. goal_status: IN_PROGRESS.

## E1.51 slices 1-7 — Prior — historical landing record.** Full E1.51 program executed slice-by-slice with checkpoint gates. All code changes are additive and backward-compatible per `CLAUDE.md §2.1` (no public API removed, `OrderflowEvent` contract untouched, `extract_pool_state_for_sim` signature untouched — registry-first lookup deferred to slice-3b for safety). Landed artifacts: (1) `m7/orderflow/pool_price_state.py` `V3PoolState` + `V2PoolState` frozen dataclasses, `decode_v3_swap_log_state` + `decode_v2_sync_log_state` decoders, thread-safe chain-scoped `PoolPriceStateRegistry` with parallel V3/V2 substores, latest-write-wins by `(block_number, log_index)`, `feed_raw_logs(chain, logs)` passive sink dispatcher, snapshot schema `m7.e1.51.slice2.pool_price_state.v2`, module-level singleton; (2) hot-lane wiring in `m7/orderflow/mode_ws_live.py` recv loop calling `feed_raw_logs(args.chain, logs)` per received block, gated by `ARBY_USE_LOCAL_PRICE_STATE` (default on); (3) `scripts/replay_v3_state_drift.py` offline drift harness (gates `drift_pools_pct < 1%` AND `max_sqrt_price_drift_bps < 5`); (4) `m7/orderflow/flashblocks_ingest.py` `consume_flashblock` + `FlashblockSubscriber` stub for pre-confirmed Base sequencer feed (real subscriber lands when provider chosen); (5) `docs/m7/SIM_BACKEND_ANVIL.md` operational runbook for switching `ARBY_SIM_BACKEND=anvil` (router was already wired in `m7/orderflow/simulation.py`, no code dup); (6) `docs/m7/SELF_HOST_BASE_NODE.md` Hetzner AX52 ~$60/mo plan, op-geth + op-node, snapshot bootstrap, monitoring matrix, break-even calc; (7) `docs/m7/E1_51_CANARY_ACCEPTANCE.md` 30m canary command + 8-row acceptance metric table + verdict mapping. Test shields: `tests/unit/test_pool_price_state.py` 37 cases (V3 decode 8 + V3 registry 9 + singleton 2 + V2 decode 6 + V2 registry 7 + feed_raw_logs 5), `tests/unit/test_replay_v3_state_drift.py` 5 cases, `tests/unit/test_flashblocks_ingest.py` 5 cases — all passing. Verification: `pytest tests/unit -q` **4495 passed / 6 skipped / 1 warning** in 97.56s (4446 baseline → +49: 19 slice-1 + 13 slice-2 + 5 slice-3 + 5 slice-4 + 5 slice-6 + 2 misc); `check_repo_safety.py` PASS 0 warnings 20 gates. **Production readiness UNCHANGED NOT MET** — slices 1-7 are infra-data plane only; slice-3b (registry-first lookup in `extract_pool_state_for_sim`) and slice-8 (live 30m canary with `price_state_updates_delta>0`, `ws_429_delta=0`, `sim_attempted_delta>0`, `clean_child_exits_delta>=1`) are not yet executed in this session — slice-8 cannot be synchronously verified per `AGENTS.md §0` ("cycles_completed=0 cannot be REACHED without fresh runtime evidence"). PROD `roundtrip_profitable_total=0` is still the M7 economics blocker. **All earlier acceptance, structural, and policy items below are unchanged.** Next operational steps: (a) ops decision on Hetzner provisioning per slice-7 doc OR procure premium Alchemy WS; (b) launch 30m canary per slice-8 acceptance doc; (c) if canary green, execute slice-3b registry-first lookup in `extract_pool_state_for_sim` with a 4th canary to confirm `multicall_full_pool_data_calls_per_scored_event` drop ≥50%. docs_reread_confirmed: true. goal_status: IN_PROGRESS.

## E1.51 slice-1 — Prior (local V3 pool price state from Swap logs — LANDED, AWAITING WIRING.) Slice-1 of the E1.51 program lands a purely additive module `m7/orderflow/pool_price_state.py` that decodes the full 5-field V3 Swap payload (`amount0`, `amount1`, `sqrtPriceX96`, `liquidity`, `tick`) — currently `m7/orderflow/events.py` parses only the two `int256` amounts and explicitly drops the remaining 3 fields with a `# sqrtPriceX96, liquidity, tick available but not needed` comment. New `V3PoolState` frozen dataclass + `PoolPriceStateRegistry` (thread-safe, chain-scoped, latest-write-wins by `(block_number, log_index)`, snapshot schema `m7.e1.51.slice1.pool_price_state.v1`) + module-level singleton `get_registry()`. **No public API change**: `OrderflowEvent` contract untouched per `CLAUDE.md §2.1`; hot lane / fast-path / profit-guard / sim-backends untouched per slice discipline. Test shield: `tests/unit/test_pool_price_state.py` 19 cases all passing. Verification: `pytest tests/unit -q` **4467 passed / 6 skipped / 1 warning** in 114.34s; `check_repo_safety.py` PASS 0 warnings 20 gates. docs_reread_confirmed: true. goal_status: IN_PROGRESS.

## E1.50 — Prior (in-process WS-target refresh bridge — HYPOTHESIS REFUTED, BLOCKER ESCALATED, ECONOMICS BLOCKED.) E1.50 implemented the deferred TD-003 surgical bridge (last-working WS endpoint cache + mid-recv-loop bridge refresh + zero-liq refresh throttling) and live-tested it via a 30 min canary 2026-04-30T20:49:39Z→21:19:41Z with `--m7-hot-ws-timeout 120 --m7-hot-blocks 300 --m7-hot-max-events 120` and `ARBY_ALCHEMY_WS_FALLBACK=1 ARBY_HOT_HEARTBEAT_INTERVAL_S=30 ARBY_HOT_BRIDGE_REFRESH_S=45 ARBY_HOT_ZLR_EVERY_N_ITERS=3 ARBY_WS_REUSE_LAST_WORKING=1`. Result: **cycles_completed=0 unchanged**, `clean_child_exits_delta=0`, `sim_passed_delta=0`, `roundtrip_attempted_delta=0` (PROD: events +174, fast_scored +31; DISC: events +325, fast_scored +42). `periodic_heartbeats_delta=+13` confirms iteration-boundary cadence telemetry IS working (so the reviewer guard now sees liveness even when WS subscribe fails); `strict_provider_breaches_total=0` confirms strict policy held. **Diagnosis**: the in-process refresh bridge fixes a different problem — it removes per-iteration redundant WS resolution and registry/cache rebuild costs — but it does NOT fix the underlying drpc 429 reconnect storm on `eth_subscribe newHeads`. The recv-loop body (where the new mid-loop bridge refresh, mid-cycle heartbeat, and reused-last-working-WS preference all live) is NEVER reached during a 429-blocked subscribe, so all three patches deliver zero observable cadence change in this canary. Work-track:
- E1.50 step A — `_LAST_WORKING_WS` module-level cache + `_remember_last_working_ws` / `_peek_last_working_ws` helpers in `m7/orderflow/mode_ws_live.py`. Stamps the proven endpoint on subscribe success, skips public_fallback, refused under strict_provider_policy. ENV gate: `ARBY_WS_REUSE_LAST_WORKING` (default `1`). Test shield: `tests/unit/test_e1_50_ws_reuse.py` (5 cases, all passing).
- E1.50 step B — mid-recv-loop bridge re-read + `_pool_token_cache` warm in `m7/orderflow/mode_ws_live.py` recv loop, hot-lane only, throttled to `ARBY_HOT_BRIDGE_REFRESH_S` (default 45s). Pure additive: cache writes only; registry mutation still confined to iteration boundary per TD-003 risk note (cross-process registry sharing requires lock primitives).
- E1.50 step C — `eth.block_number` + zero-liq refresh in `m7/orderflow/loop_runner.py` throttled to every Nth hot iteration via `ARBY_HOT_ZLR_EVERY_N_ITERS` (default 3). Cuts per-iteration drpc HTTP load by ~66% to leave headroom for WS recv loop.
- Verification: full unit **4446 passed / 6 skipped / 1 warning** in 111.16s (4441 baseline + 5 new E1.50 shield). `check_repo_safety.py --allow-intent-edit` PASS 0 warnings.
- Live canary 30 min: `m7_hot_rollup_latest.json` last_updated=2026-04-30T21:19:41Z, session_id=1e42019a (TTL persisted), windows_seen=173 (+11), events_seen_total=4786 (+168), fast_path_scored_total=632 (+31), `clean_child_exits_total=9` (delta=+0 — same as E1.49 canary), `periodic_heartbeats_total=14` (+13), `session_ws_failed_429_windows=4` (+3), `last_ws_recv_error="Too many request, try again later, code: 15"`, `last_rpc_provider=drpc`. Reviewer OVERALL_ACCEPTANCE FAIL with NO_HOT_CYCLE_COMPLETED_DURING_SOAK retained for both lanes.

**Hypothesis evaluation**: ❌ REFUTED for the canary blocker. The in-process bridge improves search-window cadence ONCE the WS recv loop runs at least once, but the canary blocker is upstream — the WS subscribe itself never succeeds long enough on drpc to deliver a clean iteration boundary. Acceptance for the next soak therefore moves to step (a): provision a non-throttled WS provider for hot lane (premium Alchemy slot or dedicated drpc plan); E1.50 patches remain valuable AS-IS because they remove per-iteration redundant cost AS SOON AS WS health permits the recv loop to run.

**Production readiness remains NOT MET**: PROD `roundtrip_profitable_total=0` unchanged. Primary blocker is now firmly classified as infra (drpc 429 reconnect storms on `eth_subscribe`), with the cadence-telemetry/refresh-bridge layers landed and ready. **Do not declare production-ready until fresh PROD `roundtrip_profitable_delta>0`**. **All earlier acceptance, structural, and policy items below are unchanged.** docs_reread_confirmed: true. goal_status: IN_PROGRESS.

## E1.49 — Prior (hot-lane speed-audit cadence telemetry + reviewer guard — CADENCE OBSERVABILITY LANDED, ECONOMICS BLOCKED, INFRA BLOCKED BY drpc 429.) Reviewer 30m-soak verdict (2026-04-30) flagged hot-cycle cadence as too slow for production search (1 clean cycle in ~7 min ws_timeout=600s; 90m-soak median ≈21.8 min). E1.49 lands surgical, additive cadence telemetry per CLAUDE.md §1.2 (no large refactor). Per-event scoring latency confirmed OK (PROD 15ms, DISC p99 16ms, target 200ms) — the bottleneck is search-window flush cadence, not scoring. Work-track:
- Step 2/3 (speed metrics + periodic flush): `m7/orderflow/hot_runtime_artifacts.py` + new helpers `heartbeat_hot_rollup_cycle(cycle_started_monotonic, chain)` and `reset_cycle_window_marker(chain)`. Rolling artifact now carries `cycle_window_started_at`, `cycle_window_elapsed_s`, `last_periodic_heartbeat_at`, `last_hot_write_at`, `periodic_heartbeats_total`. Mid-cycle heartbeat is wired in `m7/orderflow/mode_ws_live.py` after successful WS subscribe (every `ARBY_HOT_HEARTBEAT_INTERVAL_S`, default 30s); cycle-marker reset is wired in `m7/orderflow/loop_runner.py` at every hot iteration start. **Canary fix**: also bump `periodic_heartbeats_total` at iteration boundary in `loop_runner` so cadence visibility survives drpc 429 storms that prevent WS subscribe from ever reaching the recv loop.
- Step 8 (reviewer cadence guard): `scripts/reviewer_soak_summary.py` adds informational `HOT_CADENCE_TOO_SLOW periodic_heartbeats_delta=N clean_child_exits_delta=M` reason when `periodic_heartbeats_delta>=ARBY_REVIEWER_MIN_HEARTBEATS_FOR_CADENCE` (default 40 ≈ 20 min @ 30s) AND `clean_child_exits_delta<ARBY_REVIEWER_MIN_HOT_CYCLES_PER_SOAK` (default 2). Does NOT flip `ok=False` (slow cadence may still produce a valid sim_passed), only surfaces the diagnosis with the canary command suggestion.
- Step 5 canary (live evidence): supervisor 2026-04-30T19:58:48Z→20:28:50Z with `--m7-hot-ws-timeout 120 --m7-hot-blocks 300 --m7-hot-max-events 120`. Result: 5/5 alive crash_restarts=0, but `cycles_completed=0` over 30 min — `clean_child_exits_delta=0`, `periodic_heartbeats_delta=0`, only +21 fast_path_scored / +157 events vs baseline. Session diagnostics: `session_ws_failed_429_windows=3`, `session_ws_recv_error_total=12`, `recv_error_reconnect_failed=3`, `last_ws_recv_error="Too many request, try again later, code: 15"`. **Verdict**: shorter `ws_timeout=120` is NOT sufficient by itself — drpc 429 reconnect storms exhaust the timeout budget before the WS recv loop yields a clean iteration boundary. The `--m7-hot-ws-timeout 120 --m7-hot-blocks 300` combo from the original GPT recommendation needs to be paired with a healthier WS provider (Alchemy fallback was set via `ARBY_ALCHEMY_WS_FALLBACK=1` but did not displace drpc).
- Steps 4/7 (in-process registry/tier-map refresh bridge): tracked as **TD-003** in `docs/TECH_DEBT.md`. This is a real cross-process registry sharing refactor (lock primitives, race-condition audit) and must be its own iteration with its own pre-conditions and acceptance gates. Deferred per CLAUDE.md §1.2.

**Acceptance for next soak**: PROD `roundtrip_profitable_delta>0` AND (`periodic_heartbeats_delta>=40` AND `clean_child_exits_delta>=2`); for that we need either (a) a WS provider that is not throttled (premium Alchemy / dedicated drpc plan), or (b) the in-process registry refresh that decouples search-window cadence from WS-provider reconnect bursts.

Verification: full unit **4441 passed / 6 skipped / 1 warning** in 96.18s (no test-count regression vs E1.48 baseline 4441), `check_repo_safety.py --allow-intent-edit` PASS 0 warnings. New cadence telemetry validated with manual smoke (`heartbeat_hot_rollup_cycle(time.monotonic()-12.5)` produced `cycle_window_elapsed_s=12.51`, `periodic_heartbeats_total=1`).

**Production readiness remains NOT MET**: PROD `roundtrip_profitable_total=0` unchanged. Primary blocker is now infra (drpc 429 reconnect storms preventing clean cycles) on top of the existing economics blocker. **Do not declare production-ready until fresh PROD `roundtrip_profitable_delta>0`**. **All earlier acceptance, structural, and policy items below are unchanged.** docs_reread_confirmed: true. goal_status: IN_PROGRESS.

## E1.48 — Prior (intent universe widened — UNIVERSE WIDENED, ECONOMICS BLOCKED) Governance-approved expansion of `config/intent.txt` 51→60 pairs (+BRETT/USDC, BRETT/WETH, DEGEN/USDC, DEGEN/WETH, TOSHI/USDC, TOSHI/WETH, VIRTUAL/cbBTC on Base; +GMX/USDC, MAGIC/USDC on Arbitrum). `config/core_tokens.yaml` promotes BRETT/DEGEN/TOSHI/GMX/MAGIC to `productive_default: true`, `cross_dex_expected: 2`, `liquidity_tier: medium`. `m7/shared/constants.py::PREWARM_PAIRS_BASE` extended 9→12 (BRETT/WETH, DEGEN/WETH, TOSHI/WETH). `scripts/check_repo_safety.py::CALIBRATION_TIER_BASELINE` 51→64 (51 + 9 promoted + 4 head-room). 3 test files updated for new contract: `test_check_repo_safety.py` (baseline assertions), `test_e1_9_discovery_lane.py` (PREWARM list, `>=` discovery>=production), `test_tiered_intent.py` (demoted-list and exploratory-list adjustments). Intent loader confirms 22 base + 16 arbitrum_one pairs parse cleanly. `onboard_base_profit/stage2.yaml` left unchanged (separate R28.27 lineage, M5 entry point, not M7 hot/cold lane). No web research (avoided guessing addresses for tokens not in core_tokens.yaml). Verification: full unit **4441 passed / 6 skipped / 1 warning** in 102.79s, `check_repo_safety.py --allow-intent-edit` PASS 0 warnings. **Production readiness still NOT MET**: PROD `roundtrip_profitable_total=0` unchanged. **Next**: E1.49 hot-lane speed-audit. docs_reread_confirmed: true.

## E1.46 — Prior (reviewer-funnel observability + WS rate-limit classification + clean-cycle accounting — ARTIFACT/REVIEWER CONTRACT FURTHER TIGHTENED, ECONOMICS BLOCKED.)
 Closed seven of the ten post-E1.45 reviewer report items with surgical, backward-compatible changes (no large refactors). Evidence basis: 30m Base soak (supervisor 2026-04-30T11:13:58Z→11:44:01Z, 5/5 alive, crash_restarts=0) PROD events +71 / fast +3 / sim +0 / roundtrip +0; DISC events +72 / fast +2 / sim +0 — E1.45 guards (rate_basis valid, provider_blocker=0) held. Work-track:
- Step 2 (BRIDGE_HIT_NOT_SCORED fresh deltas): `m7/orderflow/hot_runtime_artifacts.py::_update_hot_rollup` now mirrors `bridge_hit_but_not_fast_scored.reason_histogram` into a session-scoped `session_bridge_hit_not_scored_reason_histogram` + `session_bridge_hit_not_scored_windows` block on the `session` dict; lifetime histogram is preserved unchanged. Reviewer can now diff fresh per-session reasons instead of cumulative. (+2 tests)
- Step 3 (NO_HOT_CYCLE_COMPLETED_DURING_SOAK): rollup-side `clean_child_exits_total` + `last_clean_child_exit_at` counter incremented inside `_flush_rollup_at_path()` only when `is_supervisor_exit=False`; reviewer FAILs acceptance with reason `NO_HOT_CYCLE_COMPLETED_DURING_SOAK` when `events_seen_delta>0` AND `clean_child_exits_delta=0` AND not `ARBY_REVIEWER_QUIET_OK=1`. Distinguishes "hot lane is alive but every child crashed before flush" from a healthy soak. (+3 tests)
- Step 4 (dRPC code 15 → rate-limit classification): `m7/orderflow/mode_ws_live.py` reconnect-failure branch now inspects `_ws_last_recv_error` after `recv_error_reconnect_failed` and tags `_ws_connection_status="failed_429"` when the last error matches `"too many request" / 'code': 15 / "code": 15 / 429 / rate limit / rate-limit`. Previously only the very-first-connect HTTP 429 incremented `session_ws_failed_429_windows`; reconnect storms (the actually observed failure mode this soak) were silently classified as `failed_other`.
- Step 5 (split supervisor_window): rollup now exposes `current_session_window` block (events_total / elapsed_minutes / events_per_minute scoped to the active session_id) AND keeps `supervisor_window` plus a `historical_window` alias. Dashboard / reviewer can use `current_session_window` to surface the actual session feed rate (~10 events/min in active windows) instead of the long-tail supervisor-window rate (~0.46/min). (+2 tests)
- Step 7 (fee 2600 Slipstream route exercise): existing parametrised `test_aerodrome_cl_fee_classified` was extended in E1.45 to cover fee=2600 with the new `2600→ts=100` mapping; verified end-to-end in this iteration that the route falls through `SLIPSTREAM_PENDING_LOOKUP:2600` / `SLIPSTREAM_SIM_READY_TOKENS_MISSING:2600:ts100` reject buckets instead of `UNSUPPORTED_FEE_TIER:UNKNOWN:2600`. Runtime exercise itself (live PROD candidate hitting a fee-2600 pool) is gated on step 1 universe expansion.
- Step 8 (raise fast-path admission target): no code change required — reviewer already enforces `fast_path_scored_delta >= ARBY_REVIEWER_MIN_FAST_PATH_SCORED` (default 20). Status_M7 acceptance criterion explicitly carries `fast_path_scored_delta >= 20` for the next 30m soak as the gate to lift the FAST_PATH_SCORED_TOO_LOW reason.
- Step 10 (replay only on fresh divergence samples): `scripts/replay_divergence_samples.py --require-fresh-session` filters samples to those whose `session_id` matches the rollup’s current session before producing replay manifests; emits `NO_FRESH_SAMPLES` rc=1 instead of replaying historical histogram entries.

**Steps 1 (factory_enumeration → tier_classifier → bounded HOT universe wiring), 6 (DISC profitable historical pair → PROD watchlist with TTL/provenance), and 9 (60m active-window soak gated on step 1) DEFERRED** per CLAUDE.md §1.2 large-refactor rule — each is a coordinated multi-module wiring (step 1) or persistence + safe migration (step 6) or a runtime exercise gated on step 1 (step 9). Each must be its own iteration with its own pre-conditions and acceptance gates.

Verification: full unit **4408 passed / 6 skipped / 1 warning** in 240.99s (+7 vs E1.45 baseline 4401), `check_repo_safety.py` PASS 0 warnings (20 gates). Targeted: `test_e142_rate_metrics.py` 9 PASS (+1), `test_m7_e1_34h_reviewer_fixes.py` 17 PASS (+4), `test_reviewer_soak_summary.py` 15 PASS (+2).

**Production readiness remains NOT MET**: PROD `roundtrip_profitable_total=0` unchanged. Primary blocker is bounded universe / admission coverage (step 1) plus dRPC WS reconnect rate-limit storms now correctly classified by step 4. **Do not declare production-ready until fresh PROD `roundtrip_profitable_delta>0`**. **All earlier acceptance, structural, and policy items below are unchanged.** docs_reread_confirmed: true.

## E1.45 — Prior (reviewer guards + DISC parity + fee 2600 mapping + bootstrap probe lane-aware — ARTIFACT/REVIEWER CONTRACT TIGHTENED, ECONOMICS BLOCKED) Closed seven of the ten P0/P1 fix-steps from the post-E1.44 reviewer report with surgical, backward-compatible changes (no large refactors). Work-track:
- Step 6 (DISC parity): `mark_supervisor_end()` in `m7/orderflow/hot_runtime_artifacts.py` now refreshes BOTH the bound rollup and its `_discovery` sibling so the DISC `rate_metrics` block also lands with the E1.43+ schema (`rate_basis=current_worker_session_delta` + delta keys) at supervisor exit, not only when m7_hot_discovery completes a cycle in-process; new helper `_flush_rollup_at_path()` extracted to keep call sites flat. Regression test added in `tests/unit/test_e142_rate_metrics.py`.
- Steps 2+3 (reviewer guards): `scripts/reviewer_soak_summary.py` now FAILs acceptance on (a) `RATE_METRICS_SCHEMA_STALE` when rollup block is missing `rate_basis=current_worker_session_delta` or any of `roundtrip_attempted_delta`/`roundtrip_profitable_delta`/`scoring_blackhole_windows_delta`; (b) `RATE_METRICS_ABSURD` when any per-hour rate exceeds `ARBY_REVIEWER_ABS_RATE_THRESHOLD_PER_HOUR` (default 1000) — catches future regressions of the E1.42 lifetime/elapsed bug class at acceptance time. Three regression tests added in `tests/unit/test_reviewer_soak_summary.py`.
- Step 5 (SCORING_BLACKHOLE breakdown): `summarise_lane()` now decomposes the SCORING_BLACKHOLE reason into `[NO_BRIDGE_HIT BRIDGE_HIT_NOT_SCORED PAIR_FILTERED TIER_COLD]` subtotals using existing rolling artifact fields (`windows_events_without_bridge_hit_total`, `bridge_hit_but_not_fast_scored.windows`, `bridge_hit_but_not_fast_scored.reason_histogram`). Test added.
- Step 7 (active-hot-write check): reviewer now surfaces `NO_HOT_WRITE_DURING_SOAK` reason when `events_seen_delta=0` and `ARBY_REVIEWER_QUIET_OK!=1`, distinguishing infra silence (drpc 429 storms / hot lane crash) from genuine market_quiet. Test added.
- Step 4 (probe lane-aware): `scripts/bootstrap_system.ps1` rollup probe now grants PASS-by-shutdown when the supervisor exits cleanly during the probe window AND the rollup carries a fresh `shutdown_flush_at`/`supervisor_end_utc` stamp (avoids killing the post-mortem reviewer summary on short-window soaks).
- Step 8 (Aerodrome fee 2600): added `2600 -> ts=100` to `SLIPSTREAM_FEE_TO_TICKSPACING` in `m7/orderflow/execution_gate.py` (medium-volatility bucket, mirrors 2105/2655/3024 family) and added 2600 to `_AERODROME_CL_KNOWN`. Existing parametrised test extended. This unblocks the PROD `SELL_FEE_UNSUPPORTED:2600` path observed in the E1.40 soak: candidates now route via the Slipstream adapter instead of falling through to UNSUPPORTED_FEE_TIER.

**Steps 9 (factory_enumeration -> tier_classifier -> bounded HOT universe wiring) and 10 (30-60m active-window soak) DEFERRED** per CLAUDE.md §1.2 large-refactor rule — step 9 requires a coordinated multi-module wiring (background tier_classifier task at WARM cadence, HOT loop reading classified universe and capping to 100-300, persistence of tier_map artifact, safe migration path) that must be its own iteration with its own pre-conditions and acceptance gates; step 10 is a runtime exercise gated on step 9 landing.

Verification: `tests/unit/test_e142_rate_metrics.py` 8 PASS (+1), `tests/unit/test_reviewer_soak_summary.py` 13 PASS (+5), `tests/unit/test_m7_e1_34h_reviewer_fixes.py` 13 PASS, `tests/unit/test_execution_gate.py` 73 PASS, full unit **4401 passed / 6 skipped / 1 warning** in 106s, `check_repo_safety.py` PASS 0 warnings (20 gates).

**Production readiness remains NOT MET**: PROD `roundtrip_profitable_total=0` unchanged — economics blocker persists pending step 9 (universe expansion) and a fresh active-window soak. **Do not declare production-ready until fresh PROD `roundtrip_profitable_delta>0`**. **All earlier acceptance, structural, and policy items below are unchanged.** docs_reread_confirmed: true.

## E1.44 — Prior (rate_metrics shutdown write-through — ARTIFACT CONTRACT FIXED, ECONOMICS BLOCKED)

Deep audit of E1.43 found that `_compute_rate_metrics()` arithmetic was correct but live `m7_hot_rollup_latest.json` could still retain the old pre-E1.43 block because `flush_rollup_shutdown()` / `mark_supervisor_end()` refreshed `last_updated` and `supervisor_end_utc` without recomputing `rate_metrics`. Evidence: fail-fast bootstrap at 2026-04-30T08:48:54Z aborted after 180s with no fresh write and reaped 6 orphan descendants; pre-patch 10m no-probe soak (2026-04-30T08:52:34Z → 09:02:56Z) completed cleanly with `m7_hot`/`m7_hot_discovery` cycles=1 but still showed stale `roundtrip_attempt_rate_per_hour=24705.8824` and no `rate_basis`; post-patch 10m no-probe soak (2026-04-30T09:05:11Z → 09:15:33Z) completed cleanly, `crash_restarts=0`, and PROD rollup now shows `rate_basis=current_worker_session_delta`, `roundtrip_attempt_rate_per_hour=0.0`, `roundtrip_attempted_delta=0`, `roundtrip_profitable_delta=0`, `scoring_blackhole_windows_delta=0`. Code change: `m7/orderflow/hot_runtime_artifacts.py::flush_rollup_shutdown()` now recomputes `rate_metrics` before every shutdown/supervisor-end atomic write; regression test added in `tests/unit/test_e142_rate_metrics.py`. Verification: `tests/unit/test_e142_rate_metrics.py` 7 PASS, `tests/unit/test_m7_e1_34h_reviewer_fixes.py` 13 PASS, full unit **4395 passed / 6 skipped / 1 warning**, `check_repo_safety.py` PASS 0 warnings (20 gates), Base RPC HTTP/archive/newHeads PASS (Flashblocks WARN only). Reviewer after post-patch soak still FAILs economically: PROD fresh deltas all zero (`events=0`, `fast_path_scored=0`, `sim=0`, `roundtrip=0`), profitability `NO_ROUNDTRIP_ATTEMPTED [DELTA_VS_BASELINE]`. **Production readiness remains NOT MET**: PROD `roundtrip_profitable_total=0`; current blocker is funnel inactivity/narrow coverage, not rate_metrics arithmetic. **Next P0**: (1) reviewer guard for `rate_basis` + delta keys + impossible rates; (2) SCORING_BLACKHOLE breakdown into `NO_BRIDGE_HIT / BRIDGE_HIT_NOT_SCORED / PAIR_FILTERED / TIER_COLD`; (3) probe semantics fix so no mid-run hot write is not confused with artifact-contract failure; (4) DISC shutdown-path rate_metrics refresh parity; (5) Aerodrome/Velodrome `pool.type` route audit for fee=2600. **Do not declare production-ready until fresh PROD `roundtrip_profitable_delta>0`**. **All earlier acceptance, structural, and policy items below are unchanged.** docs_reread_confirmed: true.

## E1.42 — Prior (6-iter sequential work-track + 60-min soak)

Implemented Iter 1-5 sequentially with intermediate test+safety gates: Iter 1 fee=2600 4-category audit in `m7/orderflow/execution_gate.py` (+4 tests); Iter 2 DISC watchlist with 4h TTL — `m7/orderflow/disc_watchlist.py` NEW (+9 tests); Iter 3 rate_metrics block in rolling artifact — `m7/orderflow/hot_runtime_artifacts.py` (+5 tests; **NOTE: shipped with bug, fixed in E1.43**); Iter 4 factory_enumeration cold collector API — `discovery/factory_enumeration.py` (+6 tests); Iter 5 tier_classifier runtime artifact — `discovery/tier_classifier.py` NEW (+9 tests). Suite 4360→4393 (+33), safety PASS at every iter. Iter 6 60-min Base soak finished cleanly (supervisor PID 4236 exited 2026-04-30T01:26:12Z, 0 crash_restarts, 5/5 children). Reviewer FAIL: market-quiet (events +40, fast_path_scored +0, sim_attempted +0). Profitability: NO_ROUNDTRIP_ATTEMPTED [DELTA_VS_BASELINE]. PROD economic state unchanged (`roundtrip_profitable_total=0`). docs_reread_confirmed: true.

## E1.41 — Prior (30-min validation soak after E1.40 fixes — MARKET-QUIET WINDOW, NO FRESH ECONOMICS)

Session 1e4d0432, 2026-04-29T21:24:57→21:55:16Z, 5/5 alive, crash_restarts=0. Fresh deltas: events +34, fast_path_scored +2, sim_attempted +0, roundtrip_attempted +0. Reviewer FAIL: `FAST_PATH_SCORED_TOO_LOW=2<20`, `NO_FRESH_SIM_PASSED`. **Diagnosis**: with the current Base active universe (15 Base pairs from `config/intent.txt` of 51 global intent entries; ~50 focused pools at hot iteration), economic events arrive at ~1 profitable opportunity per multi-hour window; a 30-min validation soak is statistically insufficient regardless of code correctness. Cumulative state preserved from E1.40 soak: PROD `roundtrip_profit_bps_best=-59.91` (`SELL_FEE_UNSUPPORTED:2600`), DISC `roundtrip_profit_bps_best=+91.0771`. **Conclusion**: short-soak validation cannot prove or refute economic breakthrough. The two non-trivial paths to PROD `roundtrip_profitable_total>0` are: (1) **dedicated multi-hour Base soak in active market window** (4–8h, no code change), (2) **Universe expansion via deferred P0 work** (factory_enumeration runtime wiring + tier_classifier scoring loop integration + DISC→PROD watchlist promotion + fee 2600 adapter audit). Both paths are explicitly DEFERRED per CLAUDE.md §1.2 large-refactor rule and require dedicated iterations with own pre-conditions and acceptance gates. **E1.41 deliverables this iteration**: validated 30-min soak runs cleanly with E1.40 fixes (zero-liq refresh + bootstrap env hygiene); no regressions in unit suite (4360 PASS / 6 skipped); `check_repo_safety.py` PASS 0 warnings (20 gates). **All earlier acceptance, structural, and policy items below are unchanged.** docs_reread_confirmed: true.

## E1.40 — Prior (zero-liq CL refresh, infra breakthrough)

 Reviewer **PASS** for PROD acceptance with explicit `--staleness-anchor-utc 2026-04-29T18:57:54Z`. Fresh PROD soak delta (session 5749662e, 2026-04-29T19:27:47→20:57:54, 5/5 alive, crash_restarts=0): events +184, fast_path_scored +154, sim_passed +3, **roundtrip_success +2 (first ever in PROD)**, MISSING_SIZE_METADATA delta=0 (P0 propagation fix CONFIRMED). DISC parallel results: roundtrip_success_total=8, **roundtrip_profitable_total=1, roundtrip_profit_bps_best=+91.0771**. **DISC profitable case identified**: pair `0x4bfaa776991e85e5f8b1255461cbbd216cfc714f`/WETH, buy on `pancakeswap_v3` fee=500, sell on `uniswap_v3` fee=10000, scoring_path=`registry_fast`, scored_net_bps=90.93, sim_output_wei=6772295847836 (≈6.77e-6 WETH per 1e18 token in). PROD best fresh roundtrip: **−59.91 bps loss** on token `0xac1bd248`; blocked by `SELL_BUILD:SELL_FEE_UNSUPPORTED:2600` (1 sample) — fee tier 2600 is not registered with any venue/factory in this codebase (not in standard V3 set {100,500,2500,3000,10000}, not in Aerodrome CL set {150,445,600,1000,2105,2655,3024,5000,20000}, not in Algebra dynamic range 1<x≤100). PROD pre-sim skip histogram delta: BELOW_MIN_NET_BPS:1.0 +2 (caused by `bootstrap_system.ps1` line 107 hardcode), NO_FEE_HINT +2; sim revert: REVERT:STF +2 (in-flight pool state changes — expected on real markets). **E1.26b zero-liq fix VALIDATED**: `registry_has_pair_but_not_pool_count: 0` for all 8 hot cycles (was 9–13). `admitted_to_scoring` improved 1–2 → 13–29/cycle. Bootstrap hardcode fixed: `bootstrap_system.ps1` line 107 changed to `if (-not $env:ARBY_SIM_MIN_NET_BPS) { ... = '1.0' }` (E1.40). E1.26b fix: `_refresh_zero_liquidity_entries()` in `m7/orderflow/bridge_runtime.py` re-queries PTT-injected concentrated-liquidity pools that had `sqrt_price_x96>0` but `liquidity=0` at prewarm time (CL positions out of tick range); periodic refresh called each hot iteration in `m7/orderflow/loop_runner.py`. P0 scoring_parallel.py fix (E1.39): `score_backrun_fast()` propagates `token_in_decimals`, `size_normalization_source`, `size_usd_estimate` to BackrunResult — delta confirms zero new MISSING_SIZE_METADATA this soak. Unit: 4360 PASS / 6 skipped (no regressions). `check_repo_safety.py` PASS 0 warnings (20 gates). **Production-readiness criteria still NOT MET** (`roundtrip_profitable_total=0` in PROD). **Next P0 (DEFERRED to dedicated iteration per CLAUDE.md §1.2)**: (a) wire `discovery/factory_enumeration.py` runtime collector for Base DEXes (Uniswap V3 / PancakeSwap V3 / Aerodrome) to expand cold universe; (b) wire `discovery/tier_classifier.py` into `discovery/runtime.py` + scoring loop so HOT pools get sub-200ms refresh while COLD pools refresh on slower cadence; (c) add "promote DISC profitable winner to PROD watchlist" path keyed on `roundtrip_profitable=true && session_id` provenance (target pair `0x4bfaa776/WETH` from this soak); (d) investigate fee tier 2600 origin (likely a misread from a non-standard adapter — needs adapter audit before accepting); (e) add adaptive size sweep around best-loss roundtrip to find profitable size band. **Acceptance for next soak**: PROD `roundtrip_profitable_delta > 0` OR top-loss closer than `-20 bps`. **All earlier acceptance, structural, and policy items below are unchanged.** docs_reread_confirmed: true.

## E1.39 — Prior (supervisor_end_utc/probe/MISSING_SIZE_METADATA fixes, compressed) Unit **4357 PASS / 6 skipped / 0 failed** (no test-count regression vs E1.38 baseline). `check_repo_safety.py` PASS 0 warnings (20 gates). 1h Base soak completed cleanly (session e14446d0 cumulative; current_session_started_at=2026-04-29T12:36:05Z, supervisor_end_utc=2026-04-29T14:42:49Z; rolling-aggregate session_id persists by design). Cumulative counters at flush: events=3352 (+267 vs prior 3085), fast_scored=414 (+24), fp_positive=18 (+2), sim_attempted=7, sim_passed=4, rt_attempted=4, rt_profitable=0, submit_ready=0, windows=103 with_events=94 with_fast=36, evs_no_fast=18, crash_restarts=0, external_provider_blocker_total=0. Submit-blocker histogram (cumulative lifetime, NOT fresh delta): `ROUNDTRIP_NOT_PROFITABLE=4`, `SCORER_SIM_DIVERGENCE 2424→-9988=3`, `SCORER_SIM_DIVERGENCE 2441→-9988=1` — **these are historical entries from the prior soak; fresh session delta = {}** (reviewer audit corrected in E1.39 pass 2). Fresh session delta from reviewer_soak_summary: `submit_blocker_histogram_delta={}`, `simulation_error_histogram_delta={PRE_SIM_SKIP:MISSING_SIZE_METADATA:+2}`, `sim_attempted_delta=0`, `roundtrip_attempted_delta=0`. `scorer_sim_divergence_samples_recent=0` (replay NO_SAMPLES confirmed). Sim-error cumulative: `MISSING_SIZE_METADATA=11`, `REVERT:unknown:no_data=2`, 1 legacy Tenderly 403. Production-readiness criteria UNCHANGED → still NOT MET (rt_profitable=0). **Iter1 fix LANDED & VERIFIED**: split `flush_rollup_shutdown(chain, is_supervisor_exit=False)` from new `mark_supervisor_end(chain)` in `m7/orderflow/hot_runtime_artifacts.py`; only the supervisor finally-block calls `mark_supervisor_end` (1 call site in `scripts/start_nonstop_runtime.py`); per-cycle child clean-exits NO LONGER mis-anchor reviewer staleness. Verified live: 38+ child clean-exits during the 1h soak yet rollup `supervisor_end_utc` was stamped exactly once at 14:42:49Z = `last_updated`, matching the new contract. **Iter2 fix LANDED**: `scripts/bootstrap_system.ps1` `RollupProbeSeconds` default 90→**180** and `RollupProbeHeartbeatSeconds` default 30→**60** (cold-warmup writes can lag 45–60s on quiet markets; previous defaults false-failed the dual-liveness gate). PARSE OK. **Iter5 fix LANDED**: probe abort path in `bootstrap_system.ps1` now enumerates supervisor descendant PIDs via Win32_Process tree BEFORE graceful-stop and force-kills any survivors after the supervisor exit, eliminating the orphan-writer race that masked iter5's first run (orphans from the aborted run were still writing to the shared rollup, hiding the new session's writes). Targeted unit suite `tests/unit/test_m7_e1_34h_reviewer_fixes.py` updated to assert: (a) `flush_rollup_shutdown` does NOT stamp `supervisor_end_utc`; (b) `mark_supervisor_end` DOES; (c) Session-A path uses `mark_supervisor_end` — 13/13 PASS. **Iter3 (factory enumeration + tier classifier wiring) DEFERRED** per CLAUDE.md §1.2 (large refactor; admission already 12.4% events scored — not the bottleneck). **Reviewer P0 audit correction (E1.39 pass 2)**: `SCORER_SIM_DIVERGENCE` in `submit_blocker_histogram` is **cumulative lifetime data, not fresh delta**. `scorer_sim_divergence_samples_recent=0`, `replay_divergence_samples.py → NO_SAMPLES`. Dashboard `live_deltas` was surfacing cumulative histograms as "live" — fixed by adding `submit_blocker_histogram_delta` and `simulation_error_histogram_delta` fields in `monitoring/dashboard_server.py` (regression test added: `tests/unit/test_dashboard_summary.py`). **Corrected P0 priority**: (P0) close `MISSING_SIZE_METADATA` for fresh candidates — this is the only fresh-delta blocker (+2 in last hour); (P1) once metadata is fixed and `sim_attempted_delta>0` re-appears, rerun soak to produce fresh `scorer_sim_divergence_samples_recent`, THEN run `replay_divergence_samples.py` for quote-model uniformization; do NOT run replay on empty samples. **MISSING_SIZE_METADATA** count grew (+2 fresh) on the long tail of less-common ERC20 symbols not yet in the heuristic-decimals fallback set; gate controlled by `ARBY_SIM_REQUIRE_SIZE_METADATA=1`. Path to production `roundtrip_profitable_total>0`: (P0) extend `_resolve_token_decimals()` multicall fallback to cover symbols beyond the current ~16-symbol heuristic set; (P1) after metadata fix, run 20-min soak to verify `sim_attempted_delta>0`; (P2) if `SCORER_SIM_DIVERGENCE` reappears fresh, run `replay_divergence_samples.py` for quote-model fix; (P3) raise feed coverage `events_per_minute` to unlock market windows. **All earlier acceptance, structural, and policy items below are unchanged.**

---

## E1.38 — Prior reviewer fix-list iteration (compressed)

 Unit 4357 PASS / 6 skipped; safety PASS. First bootstrap with heartbeat probe aborted; second run with `-NoRollupProbe` completed cleanly, session 998f31d3, `crash_restarts=0`. PROD/DISC each `events +100` but `fast_path_scored +0`, `sim_passed +0`, `roundtrip_attempted +0`. Dashboard `live_deltas` present but stale/zero-funnel. Reviewer fix-list this round: (#1 bootstrap probe) heartbeat now requires DUAL liveness — log heartbeat AND rollup file mtime advance within `RollupProbeHeartbeatSeconds`; log-only no longer extends the probe; (#2) `reviewer_soak_summary.py` emits explicit `SCORING_BLACKHOLE events_seen_delta=N fast_path_scored_delta=0` reason when feed-rate is positive but scoring funnel is empty (3 unit tests); (#3) two new top-level rollup counters `windows_events_without_fast_score_total` and `windows_events_without_bridge_hit_total` separate NO_BRIDGE_HIT (universe gap) from BRIDGE_HIT_NOT_SCORED (admission filter) at delta level (3 unit tests); (#7) top-level `current_session_started_at` mirror + `supervisor_end_utc` field (None at session start, stamped by `flush_rollup_shutdown`, reset on next session) so reviewer staleness no longer relies solely on `last_updated` (3 unit tests). docs_reread_confirmed: true. Carries forward from prior E1.38 iteration: ENV `ARBY_PRE_SIM_BISECT=1` (default 0=OFF) admits SIZE_OVER_CAP candidates by scaling amount_in_wei + best_sweep_size_wei + size_usd_estimate by `cap_usd / size_usd_estimate * 0.95` and emits `SIZE_BISECTED_TO_CAP` sample. Bootstrap param `-RollupProbeHeartbeatSeconds 30`. Tool `scripts/replay_divergence_samples.py` (offline; no samples yet because runtime hasn't produced fresh divergence). Divergence sample schema with `input_mismatch_detected: bool` + `input_mismatch_ratio: float|None`. Dashboard `/api/summary.current_scan.live_deltas` block (session_id/baseline_session_id/age_seconds/is_fresh/staleness_reason/fresh_funnel/scorer_sim_divergence totals/pre_sim_skip totals/submit_blocker_histogram/simulation_error_histogram/external_provider_blocker_total). Offline scaffolds `discovery/factory_enumeration.py` and `discovery/tier_classifier.py` ship with full unit-test coverage but are NOT wired into runtime. Reviewer item **#3 (eth_simulateV1 pending-state sim)** remains DEFERRED. Production gating criteria unchanged. **Status**: **M7.E1.37 post-2h diagnostic soak: BLOCKED BY ECONOMICS — SCORER_SIM_DIVERGENCE root cause not yet uniformized; 2h Base session 37e452d6 (2026-04-29T07:32:50Z → 09:32:59Z) clean process-wise (5/5 children alive, `crash_restarts=0`, `simulation_backend=rpc_fork`, `external_provider_blocker_total=0`) but reviewer FAIL: `NO_FRESH_SIM_PASSED`, `NO_FRESH_ROUNDTRIP_ATTEMPTED`, `FAST_PATH_SCORED_TOO_LOW=1<20`, `STALE_ROLLUP[production] age=243s>120s`. SCORER_SIM_DIVERGENCE structured reproducer ring (50, session-scoped) IMPLEMENTED on both `ExecutionGateResult.scorer_sim_divergence_samples` and `m7_hot_rollup_latest.json.scorer_sim_divergence_samples_recent` (per post-soak21 P0); fallback fields hardened in execution_gate.py and zero-sample rollup contract in hot_runtime_artifacts.py. Reviewer fix-list items #1/#5/#6 VALIDATED via earlier 4h Base soak (session c12124ff, 2026-04-28T13:25:55Z, simulation_backend=rpc_fork). **#4 NOW FULL** — `latency_budget` block lifted to rollup surface in `_update_hot_rollup` with bounded 500-sample session ring, p50/p90/p99/within_target_pct/stage_breakdown; structurally validated by 10-min control run (session b4e231a0, 2026-04-29T06:52:25Z; samples=0 because window was quiet but schema/keys present and reviewer echoes `simulation_backend=rpc_fork`, `external_provider_blocker_total=0`). **#9 IMPLEMENTED, runtime path STILL UNVERIFIED** — control10 ran with `ARBY_MAX_TRADE_USD=50` but produced 0 fresh fast scores (quiet 10-min window); 2h diagnostic soak ran with `ARBY_MAX_TRADE_USD=0` (cap OFF) so SIZE_OVER_CAP path not exercised; structural unit tests (3 tests) cover the logic. Reviewer items **#1 quote-model uniformization (P0)**, **#2/#3 (Flashblocks pendingLogs + eth_simulateV1 pending-state)**, **#4 pre-sim size bisection**, **#6/#7 (factory enumeration + tiered universe)**, **#8 dashboard live-delta panels** DEFERRED per CLAUDE.md §1.2 (each is a coordinated multi-module refactor requiring own soak). Earlier acceptance and structural items unchanged below. Reviewer fix-list items #1/#5/#6 VALIDATED via 4h Base soak (session c12124ff, 2026-04-28T13:25:55Z, simulation_backend=rpc_fork). **#4 NOW FULL** — `latency_budget` block lifted to rollup surface in `_update_hot_rollup` with bounded 500-sample session ring, p50/p90/p99/within_target_pct/stage_breakdown; structurally validated by 10-min control run (session b4e231a0, 2026-04-29T06:52:25Z; samples=0 because window was quiet but schema/keys present and reviewer echoes `simulation_backend=rpc_fork`, `external_provider_blocker_total=0`). **#9 IMPLEMENTED, runtime path STILL UNVERIFIED** — control10 ran with `ARBY_MAX_TRADE_USD=50` but produced 0 fresh fast scores (quiet 10-min window), so `PRE_SIM_SKIP:SIZE_OVER_CAP` did not trigger; structural unit tests cover the logic. Reviewer items **#2/#3/#7/#8 DEFERRED** per CLAUDE.md §1.2. Unit: **4309 passed / 6 skipped / 0 failed** (+11 new vs E1.35 baseline: 2 per-iteration latency contract + 2 rollup latency contract + 3 PRE_SIM_SKIP:SIZE_OVER_CAP + 2 SCORER_SIM_DIVERGENCE regression guard + 2 zero-sample rollup contract / fallback fields); `check_repo_safety.py` PASS 0 warnings (20 gates). 4h Base soak ran with rpc_fork pinned for PROD+DISC, archive required (`ARBY_REQUIRE_ARCHIVE=1`), paper signing enabled (`ARBY_PAPER_SIGNING=1`), `ARBY_HOT_SWEEP_ENABLE=1`, `ARBY_LATENCY_TARGET_MS=200`, `ARBY_MAX_TRADE_USD=0` (cap observability OFF default). 5/5 children alive across full window, **`crash_restarts=0`**. Cumulative session counters at last flush (13:18:56Z): `events_seen_total=2338`, `fast_path_scored_total=389`, `fast_path_positive_total=15`, `sim_attempted_total=7`, `sim_passed_total=4`, `roundtrip_attempted_total=4`, `roundtrip_success_total=4`, `roundtrip_profitable_total=0`, `submit_ready_total=0`, `external_provider_blocker_total=0`, `windows_seen=67`, `windows_with_events=58`, `windows_with_fast_scores=32`. Submit-blocker histogram dominated by **`SCORER_SIM_DIVERGENCE` (4 samples: fast-path predicted +2424–2441 bps but rpc_fork sim returned −9988 bps, indicating fast-path price model diverges from real-pool simulation under non-trivial sizes)** plus 4 `ROUNDTRIP_NOT_PROFITABLE`. `simulation_error_histogram`: `PRE_SIM_SKIP:MISSING_SIZE_METADATA=8` (down from soak19; fix #5 fallbacks effective on extended symbol set), `REVERT:unknown:no_data=2`, 1 Tenderly 403 (legacy artifact, `external_provider_blocker_total=0` confirms no fresh Tenderly hits this session). Reviewer verdict (`ARBY_REVIEWER_QUIET_OK=1`, anchor=last_updated): **FAIL/NO_FRESH_SIM_PASSED+NO_FRESH_ROUNDTRIP_ATTEMPTED** — session-fresh deltas vs baseline (3d48b072 → c12124ff): events +970, fast_path_scored +45, profit_guard_passed +2, sim_attempted/passed/roundtrip/submit_ready all +0; `simulation_backend=rpc_fork`, `external_provider_blocker_total=0`. Production-readiness criteria (`sim_passed>0 AND submit_ready>0 AND roundtrip_profitable_total>0`) NOT met. Reviewer fix-list closures this iteration: **(#1) rpc_fork canonical** (bootstrap pin + reviewer echo); **(#4) latency_budget block** (samples_total / p50/p90/p99 / max_ms / target_ms / within_target_pct / 7-stage stage_breakdown) emitted in per-iteration `m7_hot_latest.json`, on rollup surface in `m7_hot_rollup_latest.json` (bounded 500-sample session ring), and on `/api/summary.current_scan` — FULL structurally; **(#5) sizing fallbacks** extended to USDC/USDT/USDC.e/USDT.e/USDbC/PYUSD/DAI/FRAX/WETH/WBTC/cbBTC/cbETH/wstETH/rETH/tBTC heuristic decimals + coarse stable-USD fallback + `default_18_inferred` source label; **(#6) hot-path size sweep gate** via `ARBY_HOT_SWEEP_ENABLE=1` lifts `registry_direct` block; **(#9) PRE_SIM_SKIP:SIZE_OVER_CAP** via `ARBY_MAX_TRADE_USD>0` (default 0=OFF, fully backward compatible). Reviewer fixes **#2 (Flashblocks pendingLogs read path)**, **#3 (eth_simulateV1 pending-state sim)**, **#7 (factory enumeration)**, **#8 (tiered cold/warm/hot universe topology)** remain DEFERRED to dedicated iterations per CLAUDE.md §1.2/§5.1 large-refactor rule (each requires new modules/ABIs/retry policies and own soak). M7.B remains policy-closed.

Unit baseline: **4408 PASS / 6 skipped / 1 warning / 0 failed** (+48 vs E1.40: +33 from E1.42, +1 from E1.43, +1 from E1.44, +6 from E1.45, +7 from E1.46 reviewer-funnel observability / WS rate-limit classification / clean-cycle accounting). `check_repo_safety.py`: PASS 0 warnings (all 20 gates).

**Updated**: 2026-04-30 (E1.46 — reviewer-funnel observability + WS rate-limit classification + clean-cycle accounting)

**Acceptance criterion (unchanged)**:
`Δsim_passed > 0 AND Δroundtrip_attempted > 0 AND ΔBlockOutOfRangeError == 0
AND strict_provider_breaches_total_delta == 0
AND fast_path_scored_delta >= ARBY_REVIEWER_MIN_FAST_PATH_SCORED (default 20,
unless ARBY_REVIEWER_QUIET_OK=1)
AND production rollup not stale (default 120s, anchor = supervisor end
when available)` on both lanes.

**Next P0 blockers (operational; no local Anvil required)**:
(a) **run the corrected soak** — mixed backend policy that already
exists in code (`simulation.py`, `rpc_fork_backend`, tenderly→rpc_fork
fallback) and in supervisor wiring
(`scripts/start_nonstop_runtime.py`): `ARBY_SIM_BACKEND=tenderly`
for PROD + `ARBY_SIM_BACKEND_DISC=rpc_fork` for DISC. Tenderly API
credentials are in `.env`; `rpc_fork` reuses the premium Base archive
RPC already used by the WS feed. No `--with-anvil`, no
`ARBY_ANVIL_*`; the previous Alchemy monthly cap path is not used;
(b) **raise feed coverage** to hit `supervisor_window.events_per_minute
>= 5`; registry / pair-filter work unlocked by the enriched bridge
samples (`bridge_hit_but_not_fast_scored.samples[]`);
(c) **classify fresh `HOT_SKIP_UNKNOWN_PAIR`** pools listed above into
either scoring candidates or concrete non-UNKNOWN reject reasons.

**M7.B remains policy-closed** — opening requires all six of:
`fast_path_scored >= 20`, `sim_passed > 0`, `roundtrip_attempted > 0`,
`BlockOutOfRangeError == 0`, `strict_provider_breaches == 0`, rollup
not stale. The 2026-04-22 soak meets only the last three.

**Previous (M7.E1.34h code cycle)** retained: enriched
`bridge_hit_not_scored_sample` (raw pool_address / scoring_path /
actual_pair / token_in / token_out / fee_tier / venue / adapter_type)
propagated into rollup `bridge_hit_but_not_fast_scored.samples[]`
(bounded ring of 10); `rollup.supervisor_window` (first_window_at,
events_total, elapsed_minutes, events_per_minute) surviving child
restarts; `flush_rollup_shutdown(chain)` stamps `shutdown_flush_at` on
clean loop exit; `sim_failed_samples_recent` ring pruned to current
`_SESSION_ID` before each append.
**Previous (M7.E1.34g)** retained: feed-rate counters
(`session.session_events_per_minute` + `session.session_elapsed_minutes`);
bridge-drop reason classification (`HOT_SKIP_UNKNOWN_PAIR` / `NOT_SCORED`
/ `SCORING_PATH_*`); `fast_path_net_bps_histogram` (8 buckets).
**Previous (M7.E1.34f)** retained: per-lane `last_heartbeat_utc` /
`last_event_utc` / `last_scored_utc`; `bridge_hit_but_not_fast_scored`
diagnostic bucket; `--staleness-anchor-utc` CLI flag;
`fresh_failed_samples` helper.

**Previous (M7.E1.34b) note** retained: 1h reviewer soak 2026-04-21: 0 restarts; production 99 fast_scored / 21 guard_passed / 19 sim_attempted / 0 sim_passed; discovery 99 / 21 / 15 / 0; ΔVENUE_MISSING=0, ΔAMOUNT_ZERO=0 (Step 7 admission filter directionally validated); 100% fresh sim attempts failed with `BlockOutOfRangeError` from the static anvil fork. **Step 9**: `anvil_backend._resolve_anvil_block_tag()` clamps event-block > local-head to "latest"; `_eth_call_anvil` retries "latest" on `BlockOutOfRangeError`; new `refresh_anvil_fork_if_stale()` calls `anvil_reset` with a fresh `head-offset` target; `scripts/start_anvil_fork.py` runs a periodic refresher thread (ENV `ARBY_ANVIL_AUTO_REFRESH=1`, `ARBY_ANVIL_REFRESH_INTERVAL_S=60`, `ARBY_ANVIL_REFRESH_DRIFT_BLOCKS=120`) and cleans orphan `anvil.exe` on Windows via `taskkill` on exit. `scripts/analyze_roundtrip_profitability.py` now demotes cumulative verdicts to `HISTORICAL_PROFITABLE_CASE` unless run with `--session-only` or `--baseline`; `scripts/reviewer_soak_summary.py` compares a pre-soak baseline and prints ONLY fresh deltas (ASCII `delta=` for Windows compatibility).

**2026-04-20 note** (audit cycle): 30-min Base `prod+discovery` soak shows runtime activity (PROD 61 windows / 121 events / 10 fast_scored; DISC 60/133/16/1 sim_attempted → REVERT:unknown), **but no profitable roundtrip**. `roundtrip_profitable_total=0` across both lanes. **Production readiness remains blocked** by economics (scorer-vs-sim gap, thin market windows) and was previously red on repo gates (INTENT_TIER_LIMIT, DOCS_CONTENT_BLOAT). As of 2026-04-20 repo gates are GREEN (safety PASS, pytest 4174 passed, ci_full_pipeline CI PASS, M4 `--strict` offline profit PASS). Historical verbose detail archived to [archive/status/Status_M7_history.md](../../archive/status/Status_M7_history.md). E1.35 (audit-recs P0–P3) and E1.36 (Tenderly state-override) are implemented but **not yet validated on PROD lane** — PROD got 0 `sim_attempted` in the last quiet-market window; validation deferred until a busier market window or `anvil`/`rpc_fork` PROD soak.

**Open action**: run a fresh 30-min Base PROD soak on a single declared sim backend (prefer `anvil` local fork or unified `rpc_fork`) to validate E1.35/E1.36 fixes end-to-end; expand revert decoding (`Error(string)`/`Panic(uint256)`/custom selectors) before canary; do not claim production-ready until `roundtrip_profitable_total>0`.
**M7 production definition (unchanged)**: `sim_passed>0 AND submit_ready>0 AND roundtrip_profitable_total>0 AND profit_realism_status=ROUNDTRIP_PROFITABLE`. Current: **first 2 clauses TRUE, last 2 clauses FALSE в†’ NOT PRODUCTION-READY.**
**Blocker classification**: `data_collection_blocker: MEDIUM` (Tenderly credit throttling on discovery lane). `market_window_blocker: HIGH` (zero profitable roundtrips across both lanes over ~3.3k windows). `code_blocker: LOW` (runtime plumbing stable, heartbeat_on_error_windows=49 PROD / 30 DISC).
**Open action**: request `rpc_fork` soak for discovery lane to eliminate Tenderly HTTP 403 noise; archive pre-April-17 history to clear `DOCS_CONTENT_BLOAT` (current 330 lines, target в‰¤300).

**Previous (superseded) reading**: E1.29 30-min production soak GREEN on 2026-04-17 вЂ” runtime artifacts refreshed, 210 iterations, 73 N5 anchor records; `guard_passed=0` (market-expected). Retained below as historical record.

---


**Scope**: M7.A only вЂ” runtime graph sourcing, measured scoring, same-state provenance, bounded size sweep, 9 canonical blocker tags, temporal repeatability, verdict summary, universe profiles, orderflow-driven backrun replay, live block-event scoring, ws-triggered streaming replay, two-stage multicall pruning, actual-pair token resolution, coverage decomposition, bounded enrichment, oracle sanity, local-sim state, gas decomposition, stale/low-lag split, pool-class truth, V2 direct resolve, blocker tags, local-state-first pricing, factory-driven pool registry, adapter-complete pricing, registry activation in ws-live, pipeline latency optimization, profit guard + hot-mode fast path, hot-lane no-fallback + execution-readiness timing, cold/hot artifact isolation + promoted watchlist, batch pre-resolve + supervisor fix. M7.B remains closed.

---

## Strategic Focus (E1.12.1)

**Base M7 production = main lane.** Arbitrum M4 = regression/paper benchmark only. Base delivers 183x more swap events per block than Arbitrum One. Arbitrum M7 FROZEN at 5.47s. Production closure requires: sim_passed>0, submit_ready>0, profit_realism=ROUNDTRIP_PROFITABLE.

## E1.12.2 вЂ” Modularize loop + wire execution gate (DONE, compressed)

Monolith (`scripts/m7a_orderflow_loop.py`) split: 2750в†’204 lines (-93%). Modules: `runtime_io.py`, `bridge_runtime.py`, `execution_gate.py`, `profit_guard.py`, `hot_runtime_artifacts.py`, `loop_runner.py`. Execution gate wired. Discovery hot artifact path-rebinding regression found and fixed. 30mГ—2 soak evidence: prod+disc 0 restarts, clean shutdown. CI: 3881.

## E1.12.3 вЂ” Simulation Telemetry + Blocker Refinement (DONE, compressed)

Added `sim_errors`, `submit_blockers_detail` to `ExecutionGateResult`. Added `simulation_error_histogram` + `submit_blocker_histogram` to rollup. Removed SUBGRAPH_API_KEY_REQUIRED blocker. Added `gas_l1_breakdown`. Key finding: sim failures were Tenderly HTTP 403 (credits exhausted). CI: 3888.

## E1.12.4 вЂ” Anvil Backend + rpc_fork (DONE, compressed)

4 sub-steps (A-D). Backend abstraction (`ARBY_SIM_BACKEND=tenderly|anvil|rpc_fork`). Anvil soak: 40/200 sim_passed. First non-Tenderly `sim_passed > 0` via Anvil fork (block 44618144). SwapRouter02 encoding (selector `0x04e45aaf`). CI: 3924.

---

## M7.AвЂ“A.5.47s: Triangular + Orderflow + Hot Execution (CLOSED, archived)

Steps 1-8, A.4, A.5.1-5.47s all CLOSED. Triangular: NO-GRADUATE (all cycles net-negative). Orderflow backrun (Arbitrum): sequential pipeline too slow. WS-live: NOT VIABLE (2.3s/event). Hot execution: bridge truth converged. **Arbitrum M7 FROZEN** (5.47s: event_source_absence confirmed, 0 hot events in peak-hours proof). Full history available in git diff.

Modules: `engine/triangular_*.py`, `scripts/m7a_*.py`, `m7/orderflow/`. Tests: 152 triangular + 369 orderflow.

---

## M7.E1: Base Flashblocks Event-Source Pilot (OPEN)

**Hypothesis**: Base delivers abundant V3 Swap events via newHeads+logs, enabling backrun scoring impossible on Arbitrum.

**E1 pilot evidence (April 7)**: 73.4 events/block (183x Arbitrum), all same-block (lag=0), best_net=-2.28 bps, sole blocker GAS_EXCEEDS_GROSS. CI: 3698.

### E1.1вЂ“E1.7: Nonstop в†’ Contamination Fix в†’ Funnel в†’ Hot Write (CLOSED, compressed)

**E1.1вЂ“E1.4**: Hot+cold nonstop, Arbitrum contamination fix, registry vs gas separation (registry NOT blocker, gas IS blocker), convergence fields. CI: 3702в†’3741.
**E1.5вЂ“E1.7**: Funnel inversion fix, strict exec (route_viable AND size_valid), hot lane write fix. CI: 3746в†’3775.

---

### M7.E1.8вЂ“E1.9.3: Dashboard + Chain Provenance + Discovery Lane Split (CLOSED, compressed)

**E1.8+E1.8.1**: Dashboard M7 freshness, `chain`+`run_context.chain` in all artifacts, 9-key signal_counts zero dict. CI: 3790в†’3797.
**E1.9вЂ“E1.9.3**: Production/discovery lane split, namespace isolation (`*_discovery.json`), peak-hours A/B (market scarcity confirmed), session-first dashboard. CI: 3817в†’3829.

---

### M7.E1.10вЂ“E1.11: Peak-Hours A/B + Contour Cleanup + dRPC (CLOSED, compressed)

**E1.10**: 1h A/B proof (1953 windows, 0 events). Contour cleanup: removed AMONGUS dead slot, meme families diagnostic_only, AERO/cbBTC prioritized. VIRTUAL family added. 429-fallback fix: Alchemyв†’publicnode WS/HTTP automatic. Post-fix: production 230 events/46 windows, discovery 245/50. CI: 3831в†’3838.
**E1.11**: dRPC provider upgrade (`BASE_RPC`/`BASE_WSS` chain-scoped envs). dRPC WS 100% stable (0 failures), HTTP 50% 429 fallback. Discovery now scoring (31 fast_path_scored vs prior 0). CI: 3838.
**E1.12**: Premium-provider gate generalized (alchemy+drpc+infura). CI gates PASS, rolling refreshed.

**E1.12.1 artifact-semantics + start.py + 10-step audit (2026-04-11)**:
Fixes: `cold_executable_positive` semantic (route_viable AND size_valid), `start.py` single-chain auto-partial. Full audit (10 steps): premium RPC backoff (3 retries), Flashblocks URL fix, Tenderly/Subgraph scaffolding, L1 data fee first-class, stricter release semantics. 30min burn-in: production +1 guard_passed, discovery 6 fast_path_scored. CI: 3862, ALL GATES PASSED.

---

## E1.13вЂ“E1.19: Denomination Fix в†’ Pipeline Unblock в†’ Velodrome в†’ Rate Limit (DONE, compressed)

**E1.13**: Fixed `gas_cost_wei` denomination (ETH wei vs token wei). **E1.14**: 6 pipeline blockers fixed; first `sim_passed=1` in rolling. **E1.15/R40**: Flashblocks DNS (`mainnet-preconf.base.org`), AERO/WETH calibration, audits. **E1.16**: `rpc_fork` backend вЂ” stateOverrides seeding; E2E `sim_passedв†’submit_ready`. **E1.17**: Config/DEX fallback fix. **E1.18**: Velodrome `0xcac88ea9` calldata encoder + ve33 adapter split. **E1.19**: Rate-limit root causes (stale 10в†’150, prewarm skip, max_pairs=10, V2 timeout). CI ladder: 3926в†’3992.

---

## E1.24–N1+N5 — Historical detail (archived)

Detailed deltas, soak tables, and per-file change-lists for **E1.24, E1.26, E1.27, E1.28, E1.29, E5, and N1+N5**
are archived at [archive/status/Status_M7_history.md](../../archive/status/Status_M7_history.md) per DOCS_POLICY bloat rule.
- **E1.24–E1.29** (DONE) — ve33 pricing/gas/coverage, router mismatch fix, round-trip sim, runtime hardening, dynamic USD price resolver, anchor-recording hooks.

## M7.B: Atomic Multi-hop Execution (NOT STARTED)

Per `docs/step_M7.md`: Opens only if M7.A proves a repeatable measured edge better than two-leg thesis.

---

## Canonical Commands

```
py -3.11 -m pytest tests/unit -q
py -3.11 scripts/start_nonstop_runtime.py --hours 0.17 --no-m4 --m7-cold-pause 5
```

## Known Blockers

1. **EVENT-SOURCE CEILING — FROZEN (Arbitrum only)** — 47s proof confirms `event_source_absence`. Does NOT apply to Base.

Resolved (historical E1.7-E1.29): HOT LANE NOT WRITING, MARKET-WINDOW SCARCITY, ALCHEMY 429, Dashboard dead, Chain provenance, Submit-stage sim=0, SIGNING_NOT_READY, TOKEN_ADDRESS_UNKNOWN, DEX_CONFIG_MISSING, ve33 ABI mismatch, dRPC 429 INTERMITTENT, ve33 pricing/coverage broken, Aerodrome stable sim, PTT router mismatch, DISC sim revert, Scorer-vs-sim gap. Triangular arb CLOSED (-14.16 bps).
